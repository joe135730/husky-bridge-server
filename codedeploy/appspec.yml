version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: PLACEHOLDER_TASK_DEF_ARN
        LoadBalancerInfo:
          ContainerName: husky-bridge-backend
          ContainerPort: 4000
        PlatformVersion: LATEST

